{
  "name": "dynamix",
  "private": true,
  "workspaces": [
    "www",
    "examples/*",
    "packages/*"
  ],
  "scripts": {
    "publish-packages": "turbo run build test && changeset version && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "tsup": "^8.0.1",
    "turbo": "^1.11.2",
    "typescript": "^5.3.3"
  }
}
